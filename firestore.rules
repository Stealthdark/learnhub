rules_version = '2';

// All Firestore access goes through the backend (Firebase Admin SDK).
// Direct browser access is denied for all collections.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
